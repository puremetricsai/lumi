# AGENTS.md

Repository guidance is maintained in [`CLAUDE.md`](./CLAUDE.md). Each package under `internal/` and `macos/` carries its
own `CLAUDE.md` with the invariants for that directory. Read them completely and follow it before making changes in this repository.
